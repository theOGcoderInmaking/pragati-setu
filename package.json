{
    "name": "pragati-setu-monorepo",
    "private": true,
    "workspaces": [
        "frontend",
        "backend"
    ],
    "scripts": {
        "dev": "npm run dev -w frontend",
        "build": "npm run build -w frontend",
        "prisma:generate": "npm run generate -w backend",
        "prisma:push": "npm run push -w backend"
    },
    "devDependencies": {
        "prisma": "^7.4.2"
    },
    "dependencies": {
        "@prisma/client": "^7.4.2"
    }
}
